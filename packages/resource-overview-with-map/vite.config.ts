import { createWidgetConfig } from '../lib/vite.config.factory';

export default createWidgetConfig({
  name: 'OpenstadHeadlessResourceOverviewWithMap',
  entry: 'src/resourceOverviewWithMap.tsx',
});
