import React from 'react';
import './index.css';
export declare const Pill: ({ text, rounded, light, role, }: {
    text: string;
    rounded?: boolean;
    light?: boolean;
    role?: string;
}) => React.JSX.Element;
